{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:push": "dotenv -e .env -- pnpm --filter @workspace/db run push",
    "db:seed": "dotenv -e .env -- pnpm --filter @workspace/scripts run seed",
    "db:setup": "pnpm run db:up && pnpm run db:push && pnpm run db:seed",
    "api:dev": "dotenv -e .env -- pnpm --filter @workspace/api-server run dev"
  },
  "private": true,
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
